// ═══════════════════════════════════════════════════════════════
// 实现纲要数据文件模板
// 拷贝此文件后，按三个区域的提示替换内容
// 文件名：实现纲要-{项目名}.data.js（与同名 HTML 文件配对）
// ═══════════════════════════════════════════════════════════════

function 执行工作数据() {
  const api = 实现纲要;

  // ─── 区域1：加载依赖数据 ──────────────────────────────────
  // 调用 api.加载数据() 加载其他项目的实现纲要数据文件
  // 路径相对于 HTML 文件所在目录
  // 示例：api.加载数据('../项目B/实现纲要-项目B.data.js');
  // 如无跨项目依赖，此处留空


  // ─── 区域2：设置项目名称（命名空间）──────────────────────
  // 本项目所有类型自动加此前缀，避免跨项目命名冲突
  // 引用本项目类型可用短名（自动补全），引用他项目类型用 "项目名.类型名"
  api.设置项目名称('{项目名称}');


  // ─── 区域3：编写项目数据 ──────────────────────────────────
  // 先设置阶段，然后使用 api.添加类()、api.设置责任映射() 等
  // 同项目引用可用短名（自动补全），跨项目引用用全名
  // 示例：
  //   api.设置阶段(阶段.类型识别);
  //   const 订单 = api.添加类({ id: '订单', 置信度: '高' });
  //   订单.拥有('项目B.认证服务');
  //   api.设置责任映射('订单', '管理订单生命周期', ['REQ-001']);

  api.设置阶段(阶段.类型识别);  // 一阶段：类型识别 / 二阶段：接口分化 / 三阶段：实现约定

  // （在此处编写项目数据）


  console.log('✅ 工作数据已执行');
}
